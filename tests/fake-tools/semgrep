#!/usr/bin/env python3
# Hermetic fake `semgrep` for the L5 test harness. Emits an empty-but-valid
# SARIF run — exercises the "deterministic tool ran clean, coverage recorded"
# path for both fixtures.
import json
import sys

print(json.dumps({
    "version": "2.1.0",
    "$schema": "https://json.schemastore.org/sarif-2.1.0.json",
    "runs": [{
        "tool": {"driver": {"name": "semgrep", "rules": []}},
        "results": [],
    }],
}))
sys.exit(0)
